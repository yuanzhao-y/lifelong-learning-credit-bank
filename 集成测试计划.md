# 集成测试计划

## 1. 测试目标

验证 Spring Boot 后端与 MySQL、Redis、Nginx、JWT、安全配置、数据库初始化脚本之间的集成是否正确，并使用真实接口完成跨模块业务链路验证。

执行集成测试前必须先读取本文件，并按本文件中的用例编号记录结果。`测试结果.md` 中引用集成测试用例时使用 `IT-链路-序号`，例如 `IT-AUTH-01`。

## 2. 测试范围

- Docker Compose 服务：MySQL、Redis、后端、Nginx、Jenkins。
- 后端健康检查、Swagger、OpenAPI。
- 注册、登录、JWT 鉴权、权限拦截。
- 学习成果目录、认证申请、审核、学分入账。
- 转换规则、专家评审、转换申请、转换审核。
- 站内信、操作日志、数据字典缓存、公告、反馈。
- 数据库初始化和关键表数据一致性。

## 3. 测试环境要求

| 项目 | 要求 |
| --- | --- |
| Docker Compose | `docker compose ps` 可用 |
| MySQL | `llcb-mysql` healthy |
| Redis | `llcb-redis` healthy |
| 后端 | `llcb-backend` running |
| Nginx | `llcb-nginx` running |
| 后端健康 | `http://localhost:8080/api/actuator/health` 返回 `UP` |
| Nginx 健康 | `http://localhost/api/actuator/health` 返回 `UP` |
| Swagger | `http://localhost:8080/api/swagger-ui.html` 可访问 |

## 4. 测试数据准备

- 通过接口创建 `qa_` 前缀测试用户，或使用已有演示账号。
- 通过管理员接口创建成果目录、专家档案、转换规则。
- 所有测试数据加时间戳，避免与已有演示数据冲突。
- JWT 只保存在本地变量，不写入 `测试结果.md`。
- 数据库查询结果只记录计数、状态和脱敏标识，不记录敏感明文。

## 5. 测试用例表

| 用例编号 | 链路 | 测试点 | 操作步骤 | 预期结果 |
| --- | --- | --- | --- | --- |
| IT-ENV-01 | 环境 | Docker 服务状态 | 执行 `docker compose ps` | MySQL、Redis、后端、Nginx、Jenkins 均 running，MySQL/Redis healthy |
| IT-ENV-02 | 环境 | 后端健康检查 | 请求 `localhost:8080/api/actuator/health` | HTTP 200，状态 `UP` |
| IT-ENV-03 | 环境 | Nginx 代理 | 请求 `localhost/api/actuator/health` | HTTP 200，状态 `UP` |
| IT-ENV-04 | 环境 | Swagger | 访问 Swagger UI 和 OpenAPI JSON | 页面/API 可访问 |
| IT-AUTH-01 | 鉴权 | 注册接口 | 调用 `/auth/register` | 用户创建成功 |
| IT-AUTH-02 | 鉴权 | 密码登录 | 调用 `/auth/login/password` | 返回 JWT 和用户信息 |
| IT-AUTH-03 | 鉴权 | 受保护接口 | 带 JWT 调用 `/users/me` | 返回当前用户 |
| IT-AUTH-04 | 鉴权 | 未认证拦截 | 不带 JWT 调用 `/users/me` | 返回未认证响应 |
| IT-RBAC-01 | 权限 | 用户菜单 | 不同角色调用 `/users/me/menus` | 返回菜单与角色匹配 |
| IT-RBAC-02 | 权限 | API 权限 | 低权限角色访问管理员接口 | 被拒绝 |
| IT-CATALOG-01 | 成果目录 | 管理员创建成果 | 调用成果目录新增接口 | 数据库有新成果目录 |
| IT-CATALOG-02 | 成果目录 | 公开成果浏览 | 调用 `/public/outcomes` | 只返回启用成果 |
| IT-CERT-01 | 认证 | 提交认证申请 | 学习者调用 `/certifications` | 生成 pending 申请 |
| IT-CERT-02 | 认证 | 审核通过 | 审核员调用 approve | 生成 learner_outcome、credit_account、credit_flow |
| IT-CERT-03 | 认证 | 审核驳回 | 审核员调用 reject | 状态 rejected，生成消息 |
| IT-CERT-04 | 认证 | 撤回申请 | 学习者撤回 pending 申请 | 状态 withdrawn |
| IT-CREDIT-01 | 学分 | 账户查询 | 学习者调用 `/credits/account` | 余额与入账结果一致 |
| IT-CREDIT-02 | 学分 | 流水查询 | 学习者调用 `/credits/flows` | 展示 earn/freeze/deduct/unfreeze 流水 |
| IT-RULE-01 | 转换规则 | 创建规则 | 管理员创建 draft 规则 | 规则入库 |
| IT-RULE-02 | 转换规则 | 提交评审 | 调用 submit-review | 状态 reviewing |
| IT-RULE-03 | 转换规则 | 指派专家 | 调用 assign-expert | 生成评审任务和消息 |
| IT-RULE-04 | 转换规则 | 专家评审 | 专家调用 reviews 接口提交 support | 规则 effective/enabled |
| IT-CONV-01 | 转换 | 匹配规则 | 学习者调用 match-rules | 返回有效规则 |
| IT-CONV-02 | 转换 | 预览折算 | 学习者调用 preview | 返回正确 targetCredit |
| IT-CONV-03 | 转换 | 提交申请 | 学习者提交转换 | 生成 pending 申请，冻结源学分 |
| IT-CONV-04 | 转换 | 审核通过 | 审核员 approve | 生成 deduct/earn 流水和 conversion_transaction |
| IT-CONV-05 | 转换 | 审核驳回 | 审核员 reject | 解冻源学分，生成消息 |
| IT-MSG-01 | 消息 | 消息列表 | 学习者调用 `/messages` | 返回认证/转换结果消息 |
| IT-MSG-02 | 消息 | 已读状态 | 调用 read/read-all | 消息状态更新 |
| IT-LOG-01 | 日志 | 操作日志 | 管理员查询 `/admin/operation-logs` | 关键操作有日志记录 |
| IT-DICT-01 | 字典 | 字典项查询 | 调用 `/dicts/{dictCode}/items` | 返回启用字典项 |
| IT-DICT-02 | 字典 | Redis 缓存删除 | 调用删除缓存接口后再次查询 | 缓存可重建 |
| IT-ANN-01 | 公告 | 公告发布与公开查询 | 管理员发布，学习者公开查询 | 启用公告可见 |
| IT-FB-01 | 反馈 | 提交与回复 | 学习者提交，管理员回复 | 状态 processed，学习者可查看回复 |
| IT-TOS-01 | 文件 | 上传接口 | 上传小文件到 `/files/upload` | 成功返回文件元数据或明确外部依赖错误 |

## 6. 执行命令或操作步骤

基础检查：

```powershell
docker compose ps
curl http://localhost:8080/api/actuator/health
curl http://localhost/api/actuator/health
curl http://localhost:8080/api/v3/api-docs
```

构建检查：

```powershell
mvn -DskipTests package
```

数据库核对使用容器命令，密码从环境或 Compose 配置读取，不写入报告：

```powershell
docker exec llcb-mysql mysql -ullcb -p<password> llcb -e "select count(*) from sys_user;"
```

Redis 核对：

```powershell
docker exec llcb-redis redis-cli ping
```

## 7. 通过标准

- 所有基础服务可用。
- 真实接口返回状态码和业务响应符合预期。
- 数据库中关键表状态一致，不出现部分成功的脏数据。
- Redis 字典缓存可创建和刷新。
- Nginx 代理路径与后端直连路径行为一致。
- 外部 TOS 不可用时，接口返回明确错误，不泄露密钥。

## 8. 失败处理方式

- 先区分环境失败、接口失败、数据失败、权限失败和外部依赖失败。
- 环境失败先检查容器日志和端口占用。
- 接口失败记录请求路径、请求体摘要、状态码、响应体摘要。
- 数据失败记录相关表、状态和数量。
- 修复后复测对应 `IT-*` 用例。

## 9. 结果记录要求

## 10. 2026-06-25 安全与并发补充

| 用例编号 | 链路 | 场景 | 通过标准 |
|---|---|---|---|
| IT-SEC-01 | 鉴权 | 学习者调用管理员API | HTTP 403 |
| IT-SEC-02 | 资源权限 | 用户A读取用户B消息/档案/审核轨迹 | 请求被拒绝 |
| IT-SEC-03 | JWT | 修改密码后使用旧Token | 请求被拒绝 |
| IT-SEC-04 | CORS/响应头 | 非白名单Origin与Nginx响应 | 无跨域授权且安全头存在 |
| IT-CSV-01 | 成果导入 | BOM、引号、逗号、重复编码、非法行 | 合法行处理，错误行可追踪 |
| IT-CSV-02 | 成果导入 | 超过10000行或5MB | 明确拒绝且无部分写入 |
| IT-CSV-03 | 日志导出 | 条件筛选、BOM、公式转义 | 内容和筛选一致 |
| IT-CONC-01 | 认证审核 | 10个请求并行审核同一申请 | 仅一次成功且仅一笔earn |
| IT-CONC-02 | 转换审核 | 10个请求并行审核同一申请 | 仅一次成功且无重复交易 |
| IT-RECOVERY-01 | 持久化 | 重启MySQL、Redis、后端 | 数据和服务恢复 |

所有请求、响应和数据库核对结果须脱敏后保存到 `test-evidence/2026-06-25/`。

`测试结果.md` 中必须记录：

- 每个集成用例编号和结论。
- 请求路径、角色、状态码。
- 数据库核对结论。
- 涉及外部依赖时记录可用性。
- 失败项的日志位置和复测结论。
