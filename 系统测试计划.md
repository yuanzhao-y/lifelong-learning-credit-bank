# 系统测试计划

## 1. 测试目标

在接近真实使用的本地环境中验证前后端联调、角色体验、完整业务闭环、部署状态、Jenkins CI 和轻量并发表现，形成可用于实训验收的系统级结论。

执行系统测试前必须先读取本文件，并按本文件中的用例编号记录结果。`测试结果.md` 中引用系统测试用例时使用 `ST-场景-序号`，例如 `ST-E2E-01`。

## 2. 测试范围

- 前端 Vite 页面与后端 API 联调。
- 管理员、学习者、审核员、专家四类角色体验。
- 认证申请 -> 审核 -> 学分入账 -> 规则评审 -> 转换申请 -> 转换审核 -> 流水/消息/日志闭环。
- Docker Compose 部署状态。
- Jenkins Pipeline 页面和构建结果。
- JMeter 轻量并发测试。
- 报告截图素材可用性验证。

## 3. 测试环境要求

| 项目 | 要求 |
| --- | --- |
| 后端 | Docker Compose 后端或本地后端运行 |
| 前端 | `npm run dev -- --host 127.0.0.1 --port 3000` |
| 浏览器 | 可访问 `http://127.0.0.1:3000` |
| Docker | MySQL、Redis、后端、Nginx、Jenkins 运行 |
| JMeter | `D:\application\apache-jmeter-5.6.3\bin\jmeter.bat` 存在 |
| 报告素材 | `素材/` 目录已被 `.gitignore` 忽略 |

## 4. 测试数据准备

- 创建或复用 4 类角色账号：学习者、审核员、专家、管理员。
- 准备 2 条成果目录、1 条专家档案、1 条转换规则。
- 准备至少 1 条认证通过的学习者成果。
- 准备至少 1 条转换申请并走审核通过或驳回。
- 准备公告、反馈、字典项、教育经历、工作经历。

## 5. 测试用例表

| 用例编号 | 场景 | 测试点 | 操作步骤 | 预期结果 |
| --- | --- | --- | --- | --- |
| ST-DEPLOY-01 | 部署 | Docker 服务 | 执行 `docker compose ps` | 所有容器 running，MySQL/Redis healthy |
| ST-DEPLOY-02 | 部署 | 后端直连 | 访问后端健康检查 | 返回 `UP` |
| ST-DEPLOY-03 | 部署 | Nginx 代理 | 访问 Nginx 健康检查和 Swagger | 代理正常 |
| ST-DEPLOY-04 | 部署 | 数据持久化 | 重启容器后检查关键数据 | MySQL、Redis、Jenkins 数据保留 |
| ST-FE-01 | 前端 | 启动前端 | 启动 Vite 并访问首页 | 页面可访问，无空白页 |
| ST-FE-02 | 前端 | 前端构建 | 执行 `npm run build` | 构建成功 |
| ST-FE-03 | 前端 | API 代理 | 页面触发 `/api` 请求 | 请求转发到后端并成功响应 |
| ST-RBAC-01 | 角色体验 | 管理员菜单 | 管理员登录 | 显示管理类菜单 |
| ST-RBAC-02 | 角色体验 | 学习者菜单 | 学习者登录 | 显示个人中心、成果导览、认证申请等菜单 |
| ST-RBAC-03 | 角色体验 | 审核员菜单 | 审核员登录 | 显示认证审核、转换审核等菜单 |
| ST-RBAC-04 | 角色体验 | 专家菜单 | 专家登录 | 显示专家评审任务 |
| ST-E2E-01 | 核心闭环 | 注册/登录 | 创建学习者并登录 | JWT 生效，进入系统 |
| ST-E2E-02 | 核心闭环 | 认证申请 | 学习者提交成果认证 | 申请待审核 |
| ST-E2E-03 | 核心闭环 | 认证审核 | 审核员通过认证 | 生成学分账户和 earn 流水 |
| ST-E2E-04 | 核心闭环 | 规则评审 | 管理员建规则并指派专家，专家支持 | 规则生效 |
| ST-E2E-05 | 核心闭环 | 转换申请 | 学习者提交转换申请 | 源学分冻结 |
| ST-E2E-06 | 核心闭环 | 转换审核 | 审核员通过转换 | 源学分扣减，目标学分入账，生成交易 |
| ST-E2E-07 | 核心闭环 | 支撑记录 | 查看消息、日志、流水 | 消息、日志、流水与业务一致 |
| ST-E2E-08 | 核心闭环 | 驳回路径 | 认证或转换走驳回路径 | 状态、理由、消息和解冻逻辑正确 |
| ST-STATS-01 | 统计 | 管理员统计页 | 查看统计图表 | 指标和图表展示正常 |
| ST-MSG-01 | 消息 | 已读未读 | 处理站内信 | 状态和未读数正确 |
| ST-DICT-01 | 字典 | 缓存刷新 | 刷新字典缓存后页面查询 | 字典项正常展示 |
| ST-PROFILE-01 | 档案 | 教育/工作经历 | 增删改查档案 | 页面与接口一致 |
| ST-ANN-01 | 公告 | 公告发布与学习者查看 | 管理员发布公告，学习者查看 | 启用公告可见 |
| ST-FB-01 | 反馈 | 提交与回复 | 学习者提交，管理员回复 | 学习者可查看回复 |
| ST-JENKINS-01 | Jenkins | Dashboard | 打开 Jenkins | 页面可访问，不截图密码 |
| ST-JENKINS-02 | Jenkins | Pipeline | 查看或执行 `llcb-backend-ci` | 构建成功或记录插件/网络阻塞 |
| ST-JENKINS-03 | Jenkins | 构建产物 | 查看归档 Jar | 可见 `target/*.jar` |
| ST-PERF-01 | 性能 | 健康检查并发 | JMeter 20 并发 60 秒 | 错误率 0，平均响应时间记录 |
| ST-PERF-02 | 性能 | 公开查询并发 | JMeter 测公开成果或公告接口 | 错误率 0 或记录瓶颈 |
| ST-MATERIAL-01 | 素材 | 截图素材目录 | 检查 `素材/` 图片 | PNG 非 0 字节，无密钥和 token |

## 6. 执行命令或操作步骤

后端和基础设施：

```powershell
docker compose ps
curl http://localhost:8080/api/actuator/health
curl http://localhost/api/actuator/health
```

前端：

```powershell
cd credit-bank-frontend
npm install
npm run build
npm run dev -- --host 127.0.0.1 --port 3000
```

JMeter：

```powershell
D:\application\apache-jmeter-5.6.3\bin\jmeter.bat
```

如需要非 GUI 执行，先准备 `.jmx` 脚本，再执行：

```powershell
D:\application\apache-jmeter-5.6.3\bin\jmeter.bat -n -t <test-plan.jmx> -l <result.jtl>
```

## 7. 通过标准

- 用户可以通过页面完成核心业务闭环。
- 前端构建通过，页面无明显空白、路由错误或接口基础错误。
- 后端、Nginx、MySQL、Redis、Jenkins 服务状态可验证。
- Jenkins 本地 CI 有可解释结果。
- 轻量并发测试无明显错误。
- 截图素材不包含 AccessKey、JWT 明文、数据库密码或 Jenkins 初始化密码。

## 8. 失败处理方式

- 页面失败先查看浏览器网络请求，再判断前端适配或后端接口问题。
- 后端失败查看响应体、容器日志和数据库状态。
- Jenkins 失败区分插件缺失、网络问题、Jenkinsfile 问题和 Maven 构建问题。
- 性能失败记录并发数、错误率、平均响应时间和本机资源状态。
- 修复后复测对应 `ST-*` 用例。

## 9. 结果记录要求

## 10. 2026-06-25 可审计系统测试补充

| 用例编号 | 场景 | 负载/操作 | 通过标准 |
|---|---|---|---|
| ST-FE-04 | 表单与错误提示 | 登录、搜索、分页、上传、下载 | 操作真实生效，失败提示明确 |
| ST-FE-05 | 前端权限 | 四角色直接输入受限路由 | 跳转至各自首页或403页 |
| ST-PERF-03 | 公开查询业务压测 | 30用户、30秒升压、5分钟、经Nginx | 无5xx，错误率<=1%，P95<=500ms |
| ST-PERF-04 | 认证查询业务压测 | 同上，使用测试Token | 无5xx，错误率<=1%，P95<=500ms |
| ST-PERF-05 | 登录接口压测 | 受控频率，记录限流影响 | 无服务崩溃，P95<=1000ms |
| ST-SEC-01 | ZAP基线扫描 | 扫描Nginx入口 | 原始报告留存，高危项为0或明确修复 |
| ST-DEP-01 | 依赖漏洞扫描 | npm audit与Maven依赖检查 | 原始结果留存，风险分级 |
| ST-RECOVERY-01 | 容器恢复 | 逐个重启服务且不删除卷 | 业务数据和Jenkins历史保留 |
| ST-CI-01 | 远程可追溯构建 | Jenkins从`llcb/develop`构建 | SHA一致，测试、构建、归档成功 |

原`ST-PERF-01`只作为健康检查热身，不作为业务性能结论。

`测试结果.md` 中必须记录：

- 系统测试用例编号。
- 执行页面或接口。
- 角色账号。
- 实际结果和截图/日志证据位置。
- 性能测试参数和结果摘要。
- Jenkins 结果和构建产物证据。
