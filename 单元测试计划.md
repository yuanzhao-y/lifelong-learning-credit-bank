# 单元测试计划

## 1. 测试目标

通过 JUnit 5 和 Mockito 为后端核心服务、工具类和安全组件补充自动化单元测试，验证业务规则和异常分支。目标是让 `mvn test` 具有真实测试覆盖，而不是无测试源空跑。

执行单元测试前必须先读取本文件，并按本文件中的用例编号记录结果。`测试结果.md` 中引用单元测试用例时使用 `UT-类名-序号`，例如 `UT-CreditService-01`。

## 2. 测试范围

- 服务层：`CreditService`、`CertificationService`、`ConversionService`、`ConversionRuleService`、`AuthService`、`DictCacheService`。
- 安全组件：`JwtService`、`SensitiveDataService`。
- 配置辅助：`MybatisPlusConfig.page`、密码编码器、公共常量。
- 单元测试中优先 Mock Mapper、Redis、TOS、消息服务和审核轨迹服务。

## 3. 测试环境要求

| 项目 | 要求 |
| --- | --- |
| JDK | 17+，当前本机 Java 21 可执行 |
| Maven | 3.8+ |
| 测试框架 | Spring Boot Starter Test 自带 JUnit 5、AssertJ、Mockito |
| 网络 | 单元测试不依赖网络 |
| 数据库 | 单元测试不依赖真实数据库 |

## 4. 测试数据准备

- 使用 Java 对象构造用户、成果目录、申请、学分账户、学习者成果、转换规则、专家档案。
- 使用固定 BigDecimal，例如 `10.00`、`15.00`、`0.80`。
- 用 Mockito 模拟 Mapper 的 `selectById`、`selectOne`、`insert`、`updateById` 等行为。
- 当前用户 ID 由安全上下文模拟或通过测试辅助方法设置。

## 5. 测试用例表

| 用例编号 | 目标类 | 测试点 | Mock/输入 | 预期结果 |
| --- | --- | --- | --- | --- |
| UT-CreditService-01 | `CreditService` | `getOrCreateAccount` 已有账户 | `selectOne` 返回账户 | 直接返回账户，不插入 |
| UT-CreditService-02 | `CreditService` | `getOrCreateAccount` 新账户 | `selectOne` 返回 null | 插入余额为 0 的账户 |
| UT-CreditService-03 | `CreditService` | `earn` 入账 | update 成功 | balance、totalEarned 增加，插入 earn 流水 |
| UT-CreditService-04 | `CreditService` | `freeze` 可用学分不足 | outcome available 小于冻结额 | 抛出业务异常，不更新账户 |
| UT-CreditService-05 | `CreditService` | `freeze` 账户余额不足 | account balance 小于冻结额 | 抛出业务异常 |
| UT-CreditService-06 | `CreditService` | `unfreeze` 解冻 | update 成功 | balance 增加，frozenCredit 减少 |
| UT-CreditService-07 | `CreditService` | `deductFrozen` 扣减 | 冻结充足 | frozenCredit 减少，totalDeducted 增加 |
| UT-CreditService-08 | `CreditService` | 乐观锁失败 | `updateById` 返回 0 | 抛出并发修改业务异常 |
| UT-CertificationService-01 | `CertificationService` | 提交认证 | catalog enabled | 插入待审核申请和审核轨迹 |
| UT-CertificationService-02 | `CertificationService` | 提交停用成果 | catalog disabled/null | 抛出业务异常 |
| UT-CertificationService-03 | `CertificationService` | 撤回本人待审核申请 | own pending application | 状态变为 withdrawn |
| UT-CertificationService-04 | `CertificationService` | 撤回非待审核申请 | approved/rejected | 抛出业务异常 |
| UT-CertificationService-05 | `CertificationService` | 审核通过 | pending application | 生成 learnerOutcome、调用 credit earn、发消息 |
| UT-CertificationService-06 | `CertificationService` | 审核驳回 | pending application | 状态 rejected，保存原因，发消息 |
| UT-ConversionService-01 | `ConversionService` | 匹配规则 | 当前用户有效成果 | 查询 effective/enabled 规则 |
| UT-ConversionService-02 | `ConversionService` | 预览折算 | ratio 0.80，sourceCredit 10 | targetCredit 为 8.00 |
| UT-ConversionService-03 | `ConversionService` | 规则与源成果不匹配 | sourceCatalog 不同 | 抛出业务异常 |
| UT-ConversionService-04 | `ConversionService` | 提交转换 | rule usable | 插入申请并调用 freeze |
| UT-ConversionService-05 | `ConversionService` | 审核通过 | pending application | 调用 deductFrozen、earn，插入交易 |
| UT-ConversionService-06 | `ConversionService` | 审核驳回 | pending application | 调用 unfreeze，状态 rejected |
| UT-ConversionRuleService-01 | `ConversionRuleService` | 草稿提交评审 | draft rule | 状态变为 reviewing |
| UT-ConversionRuleService-02 | `ConversionRuleService` | 非草稿提交评审 | effective/reviewing | 抛出业务异常 |
| UT-ConversionRuleService-03 | `ConversionRuleService` | 指派专家 | reviewing + available expert | 插入 review，发专家任务消息 |
| UT-ConversionRuleService-04 | `ConversionRuleService` | 专家提交支持意见 | current expert matched | review reviewed，rule effective |
| UT-AuthService-01 | `AuthService` | 注册 | 新用户名/手机号 | 密码 BCrypt，创建用户 |
| UT-AuthService-02 | `AuthService` | 密码登录 | 正确密码 | 返回 token 和用户信息 |
| UT-AuthService-03 | `AuthService` | 密码登录失败 | 错误密码 | 抛出业务异常 |
| UT-JwtService-01 | `JwtService` | 生成与解析 token | 合法用户信息 | 解析出正确 subject/claims |
| UT-JwtService-02 | `JwtService` | 篡改 token | 非法 token | 验证失败 |
| UT-SensitiveDataService-01 | `SensitiveDataService` | 加密解密 | 明文手机号/身份证 | 解密后等于原文 |
| UT-SensitiveDataService-02 | `SensitiveDataService` | 哈希稳定 | 同一输入 | hash 相同，不同输入 hash 不同 |
| UT-SensitiveDataService-03 | `SensitiveDataService` | 脱敏展示 | 手机号/身份证 | 展示掩码格式 |
| UT-DictCacheService-01 | `DictCacheService` | 缓存命中 | Redis 有数据 | 不查询数据库或按实现返回缓存 |
| UT-DictCacheService-02 | `DictCacheService` | 缓存删除 | 删除指定字典缓存 | 调用 Redis delete |
| UT-MybatisPlusConfig-01 | `MybatisPlusConfig` | 分页安全值 | current/size 异常 | current 至少 1，size 最大 100 |

## 6. 执行命令或操作步骤

新增测试类建议路径：

```text
src/test/java/com/zhousheng/llcb/service/
src/test/java/com/zhousheng/llcb/security/
src/test/java/com/zhousheng/llcb/config/
```

执行命令：

```powershell
mvn test
```

单个测试类调试：

```powershell
mvn -Dtest=CreditServiceTest test
```

## 7. 通过标准

- `mvn test` 退出码为 0。
- 测试不是空跑，Surefire 输出实际测试数量。
- 核心服务正常分支和主要异常分支均有覆盖。
- Mock 验证关键 Mapper、消息、审核轨迹、学分服务调用。
- 所有断言可复现，不依赖执行顺序和真实外部服务。

## 8. 失败处理方式

- 若测试代码问题，修正测试夹具或 Mock 行为。
- 若业务代码问题，最小范围修复业务代码并保留测试。
- 若发现代码中文提示乱码，不影响功能时记录为可读性问题；影响编译或响应时修复并复测。

## 9. 结果记录要求

## 10. 2026-06-25 扩展用例

| 用例编号 | 测试对象 | 场景 | 通过标准 |
|---|---|---|---|
| UT-AuthService-04 | `AuthService` | 弱密码注册/重置/修改 | 拒绝不满足复杂度的密码 |
| UT-AuthService-05 | `AuthService` | 登录连续失败 | 记录失败次数并在阈值后限流 |
| UT-AuthService-06 | `AuthService` | 验证码发送频率 | 同手机号同场景一分钟内拒绝重复发送 |
| UT-AuthService-07 | `AuthService` | 验证码重试上限 | 失败五次后拒绝继续验证 |
| UT-CertificationService-07 | `CertificationService` | 并发审核抢占失败 | 不入账、不生成成果或消息 |
| UT-CertificationService-08 | `CertificationService` | 撤回与审核竞争 | 条件更新失败时返回业务错误 |
| UT-ConversionService-07 | `ConversionService` | 并发审核抢占失败 | 不扣减、不入账、不生成交易 |
| UT-ConversionService-08 | `ConversionService` | 非正数转换学分 | 拒绝零值和负值 |
| UT-CreditService-09 | `CreditService` | 成果条件更新失败 | 回滚并提示并发修改 |
| UT-CreditService-10 | `CreditService` | 非正数学分变动 | 不更新账户、不写流水 |
| UT-CsvSecurity-01 | `CsvSecurity` | 公式注入与特殊字符 | 公式前缀失效且CSV正确转义 |
| UT-HtmlSecurity-01 | `HtmlSecurity` | 公告存储型XSS | 删除脚本、事件和危险协议 |
| UT-TosStorageService-01 | `TosStorageService` | 空文件 | 在读取密钥前拒绝 |
| UT-TosStorageService-02 | `TosStorageService` | 超大文件 | 在读取密钥前拒绝 |
| UT-TosStorageService-03 | `TosStorageService` | 扩展名或类型伪造 | 在上传前拒绝 |
| UT-MessageService-01 | `MessageService` | 读取他人消息 | 返回资源不存在或无权访问 |
| UT-JwtAuthenticationFilter-01 | JWT过滤器 | 密码修改后的旧Token | 不建立认证上下文 |
| UT-AnnouncementController-01 | 公告控制器 | 历史脏HTML公开读取 | 返回内容已清洗 |

本轮执行使用 `mvn clean verify`，以 Surefire XML 与 JaCoCo XML/HTML 为原始证据。

`测试结果.md` 中必须记录：

- `mvn test` 总测试数、失败数、错误数、跳过数。
- 每个失败用例编号和原因。
- 新增测试类列表。
- 修改过的业务文件列表和复测结果。
