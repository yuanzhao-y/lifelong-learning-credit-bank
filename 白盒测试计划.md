# 白盒测试计划

## 1. 测试目标

从代码结构、业务规则、事务一致性和安全边界角度验证系统内部实现是否可靠。白盒测试重点不在页面表现，而在核心服务、状态机、异常分支、权限拦截、缓存和外部存储调用是否符合设计。

执行白盒测试前必须先读取本文件，并按本文件中的用例编号记录结果。`测试结果.md` 中引用白盒用例时使用 `WB-模块-序号`，例如 `WB-CREDIT-01`。

## 2. 测试范围

- `CreditService` 学分账户与流水。
- `CertificationService` 学习成果认证状态流转。
- `ConversionService` 转换申请、冻结、扣减、入账和交易。
- `ConversionRuleService` 规则提交、专家指派、专家评审。
- `AuthService`、`JwtService`、`SecurityConfig`、`JwtAuthenticationFilter`。
- `SensitiveDataService` 敏感数据加密、哈希、脱敏。
- `DictCacheService` Redis 缓存。
- `TosStorageService` TOS 文件上传。
- `OperationLogAspect` 操作日志。
- `MybatisPlusConfig` 分页、乐观锁、自动填充。

## 3. 测试环境要求

| 项目 | 要求 |
| --- | --- |
| 后端源码 | 当前工作区源码可读 |
| 构建 | `mvn -DskipTests package` 成功 |
| 单元测试 | 能执行 `mvn test` |
| Docker | MySQL、Redis、后端容器可用 |
| 记录 | 所有代码缺陷和修复写入 `测试结果.md` |

## 4. 测试数据准备

- 有效学习者、审核员、专家、管理员用户。
- 至少 2 条学习成果目录。
- 至少 1 条有效学习者成果。
- 至少 1 条转换规则草稿、评审中规则、生效规则。
- 至少 1 条认证申请和 1 条转换申请。
- Redis 中允许产生字典缓存。

## 5. 测试用例表

| 用例编号 | 代码区域 | 测试点 | 检查方式 | 预期结果 |
| --- | --- | --- | --- | --- |
| WB-CREDIT-01 | `CreditService.earn` | 入账余额和累计学分 | 单元测试 + 数据库核对 | balance 与 totalEarned 增加，生成 earn 流水 |
| WB-CREDIT-02 | `CreditService.freeze` | 冻结学分 | 单元测试 + 集成测试 | 账户余额减少，冻结增加，成果可用学分减少 |
| WB-CREDIT-03 | `CreditService.unfreeze` | 解冻学分 | 单元测试 + 集成测试 | 余额恢复，冻结减少，生成 unfreeze 流水 |
| WB-CREDIT-04 | `CreditService.deductFrozen` | 扣减冻结学分 | 单元测试 + 集成测试 | 冻结减少，累计扣减增加，生成 deduct 流水 |
| WB-CREDIT-05 | `CreditService` | 余额不足异常 | 单元测试 | 抛出业务异常，不产生流水 |
| WB-CREDIT-06 | `CreditService` | 乐观锁更新失败 | Mock `updateById` 返回 0 | 抛出“学分账户被并发修改，请重试”类业务异常 |
| WB-CERT-01 | `CertificationService.submit` | 提交认证 | 单元测试 + API | 创建待审核申请，绑定材料业务信息 |
| WB-CERT-02 | `CertificationService.withdraw` | 撤回限制 | 单元测试 | 仅待审核申请可撤回 |
| WB-CERT-03 | `CertificationService.approve` | 审核通过 | 集成测试 | 生成 learner_outcome、credit_account、credit_flow、消息、审核轨迹 |
| WB-CERT-04 | `CertificationService.reject` | 审核驳回 | 单元测试 + API | 状态 rejected，保存驳回原因并发消息 |
| WB-CERT-05 | `CertificationService.batchApprove` | 批量审核 | 单元测试 | 多个待审核申请逐个通过 |
| WB-CONV-01 | `ConversionService.matchRules` | 匹配规则 | 单元测试 | 只返回源目录匹配且 effective/enabled 的规则 |
| WB-CONV-02 | `ConversionService.preview` | 折算精度 | 单元测试 | targetCredit 按比例计算并保留 2 位 |
| WB-CONV-03 | `ConversionService.submit` | 提交转换 | 集成测试 | 生成申请并冻结源学分 |
| WB-CONV-04 | `ConversionService.approve` | 转换通过 | 集成测试 | 扣减源冻结学分，生成目标成果、earn/deduct 流水和交易 |
| WB-CONV-05 | `ConversionService.reject` | 转换驳回 | 集成测试 | 解冻源学分，申请 rejected，生成消息 |
| WB-CONV-06 | `ConversionService.ownOutcome` | 越权源成果 | 单元测试 | 非本人或无效成果不可使用 |
| WB-RULE-01 | `ConversionRuleService.submitReview` | 草稿提交评审 | 单元测试 | 只有 draft 可变为 reviewing |
| WB-RULE-02 | `ConversionRuleService.assignExpert` | 指派专家 | 单元测试 | 仅 reviewing 规则和 available 专家可指派 |
| WB-RULE-03 | `ConversionRuleService.review` | 专家权限 | 单元测试 | 只能处理绑定到当前专家用户的任务 |
| WB-RULE-04 | `ConversionRuleService.review` | 支持意见生效 | 集成测试 | support 后规则 effective 且 enabled |
| WB-AUTH-01 | `AuthService` | BCrypt 密码 | 单元测试 | 密码不明文存储，正确密码可匹配 |
| WB-AUTH-02 | `JwtService` | JWT 生成解析 | 单元测试 | token 可解析用户信息，过期/篡改 token 不通过 |
| WB-AUTH-03 | `SecurityConfig` | 公共路径 | 代码审查 + API | `/auth/**`、`/public/**`、Swagger、健康检查允许匿名 |
| WB-AUTH-04 | `JwtAuthenticationFilter` | 请求鉴权 | 集成测试 | 无 token 受保护接口 401，有效 token 通过 |
| WB-DATA-01 | `SensitiveDataService` | 加密/哈希/脱敏 | 单元测试 | 密文字段可解密，hash 稳定，展示脱敏 |
| WB-DICT-01 | `DictCacheService` | 字典缓存 | 集成测试 | 首次查询写缓存，删除缓存后可重建 |
| WB-TOS-01 | `TosStorageService` | 密钥文件读取 | 单元测试 + 环境检查 | 不硬编码密钥，缺失文件返回明确异常 |
| WB-LOG-01 | `OperationLogAspect` | 操作日志 | 集成测试 | 登录、申请、审核等关键操作写入日志 |
| WB-MP-01 | `MybatisPlusConfig` | 乐观锁插件 | 代码审查 + 单元测试 | 注册 OptimisticLockerInnerInterceptor 且分页拦截器存在 |
| WB-MP-02 | `MybatisPlusConfig` | 审计字段填充 | 单元/集成测试 | createdAt、updatedAt、deleted 等字段自动填充 |

## 6. 执行命令或操作步骤

```powershell
mvn -DskipTests package
mvn test
docker compose ps
curl http://localhost:8080/api/actuator/health
```

白盒执行顺序：

1. 阅读对应服务代码和实体/Mapper。
2. 对照用例检查正常分支、异常分支、事务边界。
3. 为可自动化的用例补充 JUnit 5/Mockito 测试。
4. 对跨组件用例使用真实接口和数据库核对。
5. 将发现的问题和修复记录写入 `测试结果.md`。

## 7. 通过标准

- 核心状态流转与需求一致。
- 事务内任一步失败不会留下不一致数据。
- 权限边界、本人数据边界、专家任务边界均有效。
- 缓存、TOS、日志等支撑组件失败时有明确异常或降级说明。
- `mvn test` 至少覆盖核心服务，不再是无测试源空跑。

## 8. 失败处理方式

- 优先定位到服务方法、条件分支或配置项。
- 若为业务缺陷，按最小范围修复并补测试。
- 若为环境或外部依赖问题，记录阻塞原因和替代验证方式。
- 修复后必须复测对应 `WB-*` 用例。

## 9. 结果记录要求

## 10. 2026-06-25 风险路径补充

| 用例编号 | 检查路径 | 验证方法 | 通过标准 |
|---|---|---|---|
| WB-CONC-01 | 认证审核状态抢占 | 单元测试 + 真实并发API | 仅一个审核请求成功 |
| WB-CONC-02 | 转换审核状态抢占 | 单元测试 + 真实并发API | 仅一次扣减、入账和交易 |
| WB-TX-01 | 学分处理事务回滚 | 异常注入 + 数据库核对 | 申请、账户、成果、流水状态一致 |
| WB-AUTHZ-01 | 消息与档案所有权 | 控制器/API测试 | 用户不能读写他人资源 |
| WB-AUTHZ-02 | 审核轨迹权限 | 控制器/API测试 | 仅本人或审核角色可访问 |
| WB-SEC-01 | 登录与验证码限流 | 单元测试 + Redis/API | 达到阈值返回429 |
| WB-SEC-02 | 公告HTML白名单 | 单元测试 + 页面验证 | 脚本与危险属性不可执行 |
| WB-FILE-01 | 上传边界校验 | 单元测试 + API | 空、超限、伪造类型均被拒绝 |
| WB-CSV-01 | 导入导出边界 | 单元测试 + API | BOM、转义、公式、行数限制正确 |
| WB-JWT-01 | Token撤销边界 | 过滤器测试 + API | 密码修改前Token失效 |

覆盖率按模块和包分别报告；检查项数不得与JUnit测试方法数混合统计。

`测试结果.md` 中必须记录：

- 白盒用例编号。
- 涉及类或方法。
- 检查方式。
- 实际结果。
- 修复文件和复测结论。
