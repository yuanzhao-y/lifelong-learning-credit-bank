# 终身学习学分银行测试交付报告

## 1. 交付结论

| 验收维度 | 结论 | 关键依据 |
| --- | --- | --- |
| 核心业务闭环 | 通过 | 认证、入账、规则评审、转换、交易、消息和日志可串联 |
| 需求模块覆盖 | 已建立并增强 | FR-01 至 FR-20 均有模块级 API/状态证据，关键浏览器路径 E2E 10/10 通过 |
| 后端自动化测试 | 通过 | 29 个测试类、111 项 JUnit/Surefire 用例，0 失败、0 错误 |
| 覆盖率门禁 | 通过 | 核心类行覆盖 83.76%、分支覆盖 60.81%，`check-core-coverage` 通过；应用逻辑分支覆盖 41.95% |
| 本地多实例并发一致性 | 通过 | 2 个后端实例分别经 8080/8082 直连，同一认证/转换审核 12 并发均仅 1 成功 |
| 浏览器关键路径 E2E | 通过 | 学习者、管理员、越权重定向、移动端布局等 10 个路径全部通过 |
| 性能本地容量基线 | 通过 | 30/60/100 用户阶梯压测 9 个组合全部通过，HTTP/JMeter 错误率 0% |
| 基础备份恢复演练 | 通过 | MySQL dump 恢复、Redis RDB 备份、Jenkins Home 清单均完成，手工恢复 RTO 25.33s |
| 安全测试 | 应用级通过，整体安全评估未完成 | 应用级安全检查通过；npm audit 通过；OSV/完整 DAST 仍受限 |
| 远程 CI | 通过 | 最终 SHA `b2291a4...` 的 Jenkins Build #13/#14 连续成功，Build #12 为 checkout 网络失败重试记录 |
| 最终建议 | 可交付 | 限定为可审计实训项目与功能演示版本，不作为生产上线批准 |

本报告不作为生产上线批准。结论只覆盖本报告列明的版本、环境、数据规模和测试范围。生产级结论仍需完整 DAST、后端依赖漏洞数据闭环、生产等价容量测试、完整灾备与长期 CI 稳定性监控。

## 2. 被测版本与证据链

| 项目 | 内容 |
| --- | --- |
| 测试日期 | 2026-06-25 |
| 当前工作基线 | `ebc7553e867c56fc5e98ed0dcd9d9d7fb95da55d` 加本轮测试/证据整改 |
| 最终 CI SHA | `b2291a4f13a9911613919306ccaf7307fb270d4d` |
| 最终 Jenkins 构建 | Build #13、Build #14 连续成功；Build #12 为 GitHub TLS checkout 失败重试记录 |
| 历史功能/安全测试 SHA | `2480b084bc99813cdcaa0d265fabf937503a680d` |
| 基础设施 CI SHA | `16bbf1ed9afc1265dc03001cf6a0fc96c83a800a` |
| 上轮 CI 门禁收口 SHA | `30fcd870310826c87845b2e284ac34e72ec6e87c` |
| 本轮本地 Jar SHA-256 | `331679dc947dfd5b1886917af7ba0cc2c41dae445a21b5bdad1384237427a5b3` |
| 证据包 | `test-evidence/2026-06-25` |
| 证据完整性 | `manifest.sha256` 将在最终文件更新后重新生成 |

`2480b084...` 到 `16bbf1ed...` 的业务源码继承关系见 `test-evidence/2026-06-25/source-diff-2480-to-16bb.md`。本轮新增了测试代码、QA 脚本、证据文件和 Jenkins 前端依赖审计步骤；最终以 `b2291a4...` 作为 CI 收口版本。CI 后仅补写报告、证据索引、Jenkins 摘要和 manifest，差异说明见 `source-diff-final-ci-to-report.md`。

## 3. 执行统计

| 类别 | 执行对象 | 执行 | 通过 | 失败 | 受限/说明 |
| --- | --- | ---: | ---: | ---: | --- |
| 后端单元/组件测试 | JUnit/Surefire 用例 | 111 | 111 | 0 | 新增 Controller、异常处理、安全工具分支测试 |
| API 功能/黑盒 | 自动审计检查项 | 26 | 26 | 0 | 历史 API 审计证据保留 |
| 应用级安全 | 自动审计检查项 | 18 | 18 | 0 | 认证、授权、输入、XSS、CORS、安全头 |
| 本地多实例并发 | 自动审计检查项 | 5 | 5 | 0 | 2 后端实例，认证/转换各 12 并发 |
| 浏览器关键路径 E2E | Playwright/Chrome 检查项 | 10 | 10 | 0 | 含移动端 390px 截图证据 |
| 性能阶梯压测 | JMeter 场景组合 | 9 | 9 | 0 | 30/60/100 用户，HTTP/JMeter 错误率 0% |
| 基础备份恢复 | 恢复检查项 | 6 | 6 | 0 | MySQL 恢复、Redis 备份、Jenkins 清单 |
| 依赖安全 | npm、Maven tree、OSV | 3 | 2 | 0 | OSV 受限，不声明后端依赖全清 |
| DAST | ZAP API scan | 1 | 0 | 0 | 导入 120 URL，900 秒超时，LIMITED |
| 前端构建与审计 | npm ci/build/audit | 3 | 3 | 0 | 大 chunk 与第三方 pure annotation 警告保留 |
| 远程 CI | Jenkins | 3 | 2 | 1 | #12 checkout 网络失败；#13/#14 同 SHA 连续成功 |

## 4. 覆盖率

| 范围 | 行覆盖率 | 分支覆盖率 | 结论 |
| --- | ---: | ---: | --- |
| 后端整体原始 JaCoCo | 51.62% | 8.36% | 记录实际值；实体/Lombok 生成分支显著拉低整体分支 |
| 应用逻辑范围（service/security/controller/common） | 60.95% | 41.95% | 新增补测后的主要审计口径 |
| service + security + controller | 59.90% | 47.74% | 关键应用层分支已明显高于整体原始值 |
| `security` 包 | 92.20% | 75.00% | 达标 |
| `controller` 包 | 29.54% | 32.84% | 已补强，仍是后续重点 |
| `common` 包 | 92.59% | 16.36% | 异常/响应分支仍受生成分母影响 |
| 选定核心服务与安全组件 | 83.76% | 60.81% | 达到 JaCoCo 构建门禁 |

核心门禁范围仍为 `CreditService`、`CertificationService`、`ConversionService`、`ConversionRuleService`、`AuthService`、`SensitiveDataService`、`JwtService`、`JwtAuthenticationFilter` 和 `LoginAttemptService`，门槛为行覆盖率不低于 80%、分支覆盖率不低于 55%。本轮新增测试后 `mvn -B clean verify` 已通过。整体原始分支覆盖率仍偏低，因此报告不把“全后端分支覆盖”包装为达标，只把应用逻辑范围作为改进证据。

## 5. 关键改进证据

| 缺口 | 本轮优化 | 证据 |
| --- | --- | --- |
| 功能测试与最终 CI SHA 不完全相同 | 最终 CI SHA 已锁定为 `b2291a4...`；CI 后仅文档/证据更新，不改产品源码 | `version-manifest.md`、`artifact-lineage.md`、`source-diff-final-ci-to-report.md` |
| 整体分支覆盖率低 | 新增 17 项测试，覆盖 Controller、异常、鉴权工具、过滤器分支 | `coverage-summary.md`、`surefire/TEST-*.xml` |
| DAST/后端依赖扫描未完成 | npm audit 与 Maven dependency tree 完成；ZAP 导入 120 URL 后 900 秒仍超时，OSV 受限 | `dependency-security-final.md`、`zap-final-summary.md` |
| 浏览器自动化不是全量 E2E | 升级为关键路径 E2E，学习者/管理员/越权/移动端 10/10 通过 | `frontend-e2e.md`、`frontend-e2e-mobile-admin-outcomes.png` |
| 并发仅证明单节点 | 新增 2 后端实例本地多节点并发审计 | `multinode-concurrency.md` |
| 性能不能外推生产容量 | 新增 30/60/100 用户阶梯压测，并明确仅为本地容量基线 | `performance-stepped-summary.md` |
| 持久化不是灾难恢复 | 新增 MySQL dump 恢复、Redis RDB 备份、Jenkins 清单验证 | `backup-restore-audit.md` |
| CI 稳定性专项 | 最终 SHA Jenkins #13/#14 连续成功；#12 checkout 网络失败已留痕 | `jenkins-build-12.md`、`jenkins-build-13.md`、`jenkins-build-14.md`、`jenkins-stability.md` |

## 6. 并发、性能与恢复边界

本轮多实例并发验证使用两个本地后端实例（8080、8082）连接同一 MySQL/Redis。认证审核与转换审核各 12 个并发请求均只有 1 个成功，其余请求被业务拒绝且无 500。该结论证明本地多实例共享数据库条件下的竞态防护有效，不等同于生产分布式部署的全局容量、跨机房一致性或故障转移能力。

阶梯压测覆盖 30、60、100 用户三个档位和三个业务场景。100 用户下公开成果查询 P95 28ms、学分账户 P95 33ms、密码登录 P95 113ms，HTTP/JMeter 错误率均为 0%。Docker CPU 以单个逻辑核心为 100%，历史 188.39% 约表示 1.88 个逻辑核心。该结果只支持“当前本地环境容量基线”，不支持推导生产并发容量。

基础恢复演练完成 MySQL dump 到临时容器恢复、Redis RDB 备份可观察、Jenkins Home Job/Build 清单留存，手工恢复 RTO 为 25.33 秒，RPO 为审计时点手工 dump/snapshot。它不是完整灾难恢复认证，未覆盖主机丢失、异地恢复、自动切换、TOS 对象恢复和 Jenkins Home 迁移。

## 7. 安全结论

应用级安全检查通过：认证、授权、水平越权、JWT 失效、登录/SMS 限流、SQL-shaped 输入、XSS 清洗、CSV 注入、上传伪造、CORS 和安全头均有自动化证据。前端 `npm audit --audit-level=low` 通过，Maven runtime dependency tree 已生成。

整体安全评估仍未完成：OSV 扫描受工具/仓库解析限制，ZAP final 使用改写后的 OpenAPI 文件导入 120 URL，但主动扫描 900 秒超时，只能作为 LIMITED 证据。报告不得表述为生产安全认证、完整 DAST 通过或后端依赖漏洞全清。

## 8. CI 收口要求

本轮本地已完成：`mvn -B clean verify`、`npm ci`、`npm run build`、`npm audit --audit-level=low`、关键路径 E2E、多实例并发、阶梯压测、基础恢复、依赖安全汇总和 ZAP final LIMITED 证据。

Jenkins 最终收口已经完成：

1. Build #12 拉取 `b2291a4...` 时 GitHub HTTPS checkout 出现 TLS 连接中断，未进入 Maven、前端、Compose 或归档阶段，记为基础设施网络失败。
2. Build #13 和 Build #14 在同一 SHA `b2291a4...` 连续成功，均包含 111 项后端测试、JaCoCo 核心门禁、前端 `npm ci/audit/build`、Compose 校验和 artifact 归档。
3. `scripts/qa/collect-jenkins-stability.mjs` 已生成 `jenkins-stability.md/json`，结果为 PASS。
4. CI 后只修改文档、证据索引、Jenkins 摘要和 manifest，不影响业务源码和测试结论，见 `source-diff-final-ci-to-report.md`。

## 9. 遗留风险

| 风险 | 等级 | 当前影响 | 缓解措施 | 后续里程碑 |
| --- | --- | --- | --- | --- |
| ZAP 主动扫描未在 900 秒内完成 | 中 | 无完整 DAST 通过结论 | ZAP 导入 120 URL 的 LIMITED 证据、应用级安全用例通过 | 安全专项延长扫描窗口并配置认证上下文 |
| OSV 后端依赖扫描受限 | 中 | 后端依赖漏洞清单不完整 | npm audit 与 Maven dependency tree 已完成 | 使用稳定镜像/缓存完成 OSV 或 Dependency-Check 数据库 |
| 全后端原始分支覆盖率仍低 | 中 | 实体/生成代码和非核心分支仍拉低总体 | 核心门禁通过，应用逻辑分支 41.95% | 持续补 Controller、DTO validation、异常路径测试 |
| 浏览器 E2E 非全量验收 | 中 | 未覆盖所有表单控件和所有 FR 验收细则 | 关键路径 10/10 通过，API 覆盖 FR-01 至 FR-20 | 扩展为完整 Playwright suite |
| 性能结果不能外推生产 | 中 | 不能作为生产容量承诺 | 阶梯压测仅标记为本地基线 | 生产等价资源、数据量和网络拓扑下复测 |
| 恢复演练非完整灾备 | 中 | 不覆盖主机/区域级故障 | 基础备份恢复已完成 | 设计 RTO/RPO 目标和自动恢复演练 |
| TOS 生命周期未完整覆盖 | 中 | 删除、私有访问、对象丢失未闭合 | 上传和对象可达已验证 | 文件服务专项 |

“遗留 0”仅限本轮测试范围内已确认产品缺陷均已关闭，不表示未测区域没有未知缺陷。

## 10. 验收建议

项目已达到可审计实训项目与功能演示版本的交付要求。验收时建议重点展示：核心业务闭环、应用级安全防护、多实例并发一致性、关键路径 E2E、阶梯性能基线、基础备份恢复和 Jenkins 最终 SHA 连续构建证据。

提高到生产级结论前，应完成完整 DAST、后端依赖漏洞扫描、全量浏览器 E2E、生产等价性能测试、TOS 生命周期测试、完整灾难恢复演练和长期 CI 稳定性监控。

详细原始证据入口：`test-evidence/2026-06-25/evidence-index.md`。
