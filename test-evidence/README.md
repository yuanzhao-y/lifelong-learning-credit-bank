# Test Evidence

Auditable test artifacts are grouped by execution date. Evidence files must not contain passwords, JWTs, access keys, verification codes, or unmasked personal data.

The `2026-06-25` evidence set is generated by the scripts under `scripts/qa` and is referenced by `测试结果.md` and `测试交付.md`.
